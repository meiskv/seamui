{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "form",
  "type": "registry:ui",
  "title": "Form",
  "description": "Form built on Base UI that collects field validity and focuses the first invalid control on submit.",
  "dependencies": [
    "@base-ui/react"
  ],
  "registryDependencies": [
    "https://seamui.dev/r/utils.json"
  ],
  "files": [
    {
      "path": "registry/seam/ui/form.tsx",
      "content": "\"use client\"\n\nimport type * as React from \"react\"\nimport { Form as BaseForm } from \"@base-ui/react/form\"\n\nimport { cn } from \"@/lib/utils\"\n\n// A form that collects Field validity and focuses the first invalid control on\n// submit. Pair with Field for per-row labels/errors; pass `errors` +\n// `onClearErrors` to surface server-side validation.\nfunction Form({ className, ...props }: React.ComponentProps<typeof BaseForm>) {\n  return (\n    <BaseForm\n      data-slot=\"form\"\n      className={cn(\"flex flex-col gap-6\", className)}\n      {...props}\n    />\n  )\n}\n\nexport { Form }\n",
      "type": "registry:ui",
      "target": "components/ui/form.tsx"
    }
  ]
}